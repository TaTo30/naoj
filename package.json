{
  "name": "naoj-workspace",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm",
  "engine": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter naoj run dev",
    "build": "pnpm --filter naoj run build",
    "preview": "pnpm --filter naoj run preview"
  }
}
